#
# 京东云一代 (MT7621) ImmortalWrt 云编译固件
# 主题: Argon | 语言: 中文 | 可自定义 IP 和密码
#
# 设备名: jdcloud_re-sp-01b (ImmortalWrt 分支支持)
#

name: 京东云一代 ImmortalWrt 编译 (Argon/中文)

on:
  workflow_dispatch:
    inputs:
      ip_address:
        description: '默认 IP 地址 (留空使用 192.168.12.1)'
        required: false
        default: '192.168.12.1'
        type: string
      root_password:
        description: 'Root 密码 (留空表示无密码)'
        required: false
        default: ''
        type: string
      firmware_version:
        description: '固件版本号 (可选)'
        required: false
        default: 'custom'
        type: string

env:
  IMMORTALWRT_VERSION: '23.05'
  TARGET_DEVICE: 'jdcloud_re-sp-01b'

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装编译依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential gawk gcc-multilib flex git gettext \
            libncurses-dev libssl-dev python3-distutils python3-setuptools \
            rsync unzip zlib1g-dev file wget

      - name: 下载 ImmortalWrt 源码
        run: |
          git clone --depth 1 -b openwrt-${{ env.IMMORTALWRT_VERSION }} \
            https://github.com/immortalwrt/immortalwrt.git immortalwrt 2>&1 || \
          git clone --depth 1 -b master \
            https://github.com/immortalwrt/immortalwrt.git immortalwrt

      - name: 更新 feeds
        working-directory: immortalwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 添加 iStore 软件中心
        working-directory: immortalwrt
        run: |
          echo 'src-git istore https://github.com/linkease/istore.git;main' >> feeds.conf.default
          ./scripts/feeds update istore
          ./scripts/feeds install luci-app-store

      - name: 添加 Argon 主题
        working-directory: immortalwrt
        run: |
          cd package
          git clone --depth 1 https://github.com/jerrykuku/luci-theme-argon.git
          git clone --depth 1 https://github.com/jerrykuku/luci-app-argon-config.git
          cd ..

      - name: 配置固件
        working-directory: immortalwrt
        run: |
          cat > .config << 'EOF'
          # 目标设备: 京东云一代 MT7621
          CONFIG_TARGET_ramips=y
          CONFIG_TARGET_ramips_mt7621=y
          CONFIG_TARGET_ramips_mt7621_DEVICE_jdcloud_re-sp-01b=y

          # LuCI 界面
          CONFIG_PACKAGE_luci=y

          # iStore 软件中心
          CONFIG_PACKAGE_luci-app-store=y

          # Argon 主题
          CONFIG_PACKAGE_luci-theme-argon=y
          CONFIG_PACKAGE_luci-app-argon-config=y

          # 中文语言包
          CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-opkg-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-upnp-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-wol-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-ddns-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-vnstat-zh-cn=y

          # 常用工具
          CONFIG_PACKAGE_luci-app-firewall=y
          CONFIG_PACKAGE_luci-app-opkg=y
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_luci-app-wol=y
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_luci-app-vnstat2=y

          # 网络工具
          CONFIG_PACKAGE_luci-app-nlbwmon=y
          CONFIG_PACKAGE_luci-app-ttyd=y

          # 无线驱动
          CONFIG_PACKAGE_kmod-mt7603=y
          CONFIG_PACKAGE_kmod-mt76x2=y
          CONFIG_PACKAGE_wpad-openssl=y

          # 文件系统
          CONFIG_PACKAGE_block-mount=y
          CONFIG_PACKAGE_fdisk=y
          CONFIG_PACKAGE_e2fsprogs=y

          # 其他工具
          CONFIG_PACKAGE_htop=y
          CONFIG_PACKAGE_nano=y
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_wget=y
          CONFIG_PACKAGE_openssh-sftp-server=y
          EOF

          # 应用配置
          make defconfig

      - name: 设置默认 IP
        working-directory: immortalwrt
        if: github.event.inputs.ip_address != '192.168.1.1'
        run: |
          sed -i "s/192.168.1.1/${{ github.event.inputs.ip_address }}/g" \
            package/base-files/files/bin/config_generate

      - name: 设置 Root 密码
        working-directory: immortalwrt
        if: github.event.inputs.root_password != ''
        run: |
          PASSWORD=$(openssl passwd -1 "${{ github.event.inputs.root_password }}")
          sed -i "s/root::0:0:99999:7:::/\$PASSWORD/g" \
            package/base-files/files/etc/shadow

      - name: 开始编译
        working-directory: immortalwrt
        run: |
          echo "开始编译..."
          make -j$(nproc) || make -j1 V=s

      - name: 整理固件文件
        run: |
          mkdir -p firmware
          find immortalwrt/bin/targets/ -name "*.bin" -o -name "*.img" | \
            xargs -I {} cp {} firmware/
          ls -lh firmware/

      - name: 上传固件
        uses: actions/upload-artifact@v4
        with:
          name: 京东云一代-ImmortalWrt-Argon-中文-${{ github.event.inputs.firmware_version }}
          path: firmware/
          retention-days: 30
